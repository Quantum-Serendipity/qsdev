tier_order: [baseline, enhanced, specialized]

tiers:
  baseline:
    - ripsecrets
    - gitleaks
    - check-added-large-files
    - no-commit-to-branch
    - check-merge-conflicts

  enhanced:
    - semgrep
    - shellcheck
    - formatters

  specialized:
    - lock-file-audit
    - nix-secrets-check
    - statix
